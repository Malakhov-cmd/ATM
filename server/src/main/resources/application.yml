spring:
  profiles:
    active: ${PROFILE}
  datasource:
    username: postgres
    password: Orel-5287
  jpa:
    show-sql: true
    generate-ddl: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: validate

server:
  port: 9090